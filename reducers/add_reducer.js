import { DATA_CREATE } from '../actions/types';

const INITIAL_STATE = {
    new_data:[]
};

export default (state=INITIAL_STATE, action) => {
    switch (action.type) {
        default: 
            return state;
    }
}